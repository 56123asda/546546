-- [[ 小傑獨立靜默自瞄腳本 - 安全修復版 ]] 
-- 說明：此腳本為獨立運作，已修正會導致遊戲 UI 消失的問題。

local Players = game:GetService("Players") 
local player = Players.LocalPlayer 
local UserInputService = game:GetService("UserInputService")  
local RunService = game:GetService("RunService") 
local RS = game:GetService("ReplicatedStorage") 

-- [[ 配置變數 ]] 
local SilentAimPlayersEnabled = false 
local PredictionEnabled = true 
local PredictionAmount = 0.12 
local maxRange = 1000 
local PlayersPosition = nil 

-- 技能過濾清單 
local Skills = {"Z", "X", "C", "V"} 
local Booms = {"TAP", "MainClick"} 

-- [[ 1. 核心邏輯 ]] 
local function getHRP(model) 
    return model and model:FindFirstChild("HumanoidRootPart") 
end 

local function getPredictedPosition(targetChar) 
    local hrp = getHRP(targetChar) 
    if not hrp then return nil end 
    local velocity = hrp.Velocity 
    if not PredictionEnabled or velocity.Magnitude < 2 then 
        return hrp.Position 
    end 
    return hrp.Position + (velocity * PredictionAmount) 
end 

local function getClosestPlayer() 
    local closest = nil 
    local dist = maxRange 
    local lpHRP = getHRP(player.Character) 
    if not lpHRP then return nil end 

    for _, p in ipairs(Players:GetPlayers()) do 
        if p ~= player and p.Character and p.Character:FindFirstChild("Humanoid") then 
            if p.Character.Humanoid.Health > 0 then 
                local targetHRP = getHRP(p.Character) 
                if targetHRP then 
                    local d = (targetHRP.Position - lpHRP.Position).Magnitude 
                    if d < dist then 
                        dist = d 
                        closest = p 
                    end 
                end 
            end 
        end 
    end 
    return closest 
end 

-- [[ 2. 安全修正版 Hook ]] 
-- 修復重點：加入 Remote 名稱過濾，防止攔截遊戲 UI 與系統相關的請求
spawn(function() 
    local ok, hookMeta = pcall(getrawmetatable, game) 
    if ok and hookMeta then 
        setreadonly(hookMeta, false) 
        local OldHook 
        OldHook = hookmetamethod(game, "__namecall", function(self, ...) 
            local method = getnamecallmethod() 
            local args = {...} 
            
            if SilentAimPlayersEnabled and PlayersPosition and not checkcaller() then 
                -- 僅針對特定的戰鬥遠端事件進行修正，防止影響遊戲介面
                local name = self.Name
                local isCombat = (name == "CommE" or name == "RegisterHit" or name == "RegisterAttack")

                if isCombat and (method == "FireServer" or method == "InvokeServer") then 
                    if typeof(args[1]) == "Vector3" then 
                        args[1] = PlayersPosition 
                        return OldHook(self, unpack(args)) 
                    elseif type(args[1]) == "string" and (table.find(Skills, args[1]:upper()) or table.find(Booms, args[1])) then 
                        if #args >= 2 then 
                            args[2] = PlayersPosition 
                        end 
                        return OldHook(self, unpack(args)) 
                    end 
                end 
            end 
            return OldHook(self, ...) 
        end) 
        setreadonly(hookMeta, true) 
    end 
end) 

-- [[ 3. 渲染與追蹤迴圈 ]] 
RunService.Heartbeat:Connect(function() 
    if not SilentAimPlayersEnabled then 
        PlayersPosition = nil 
        return 
    end 

    local target = getClosestPlayer() 
    if target and target.Character then 
        PlayersPosition = getPredictedPosition(target.Character) 
    else 
        PlayersPosition = nil 
    end 
end) 

-- [[ 4. UI 介面設定 ]] 
local ScreenGui = Instance.new("ScreenGui") 
ScreenGui.Name = "XiaoJieAimbot" 
ScreenGui.ResetOnSpawn = false 
ScreenGui.Parent = player:WaitForChild("PlayerGui") 
 
local MainFrame = Instance.new("Frame") 
MainFrame.Size = UDim2.new(0, 220, 0, 130) 
MainFrame.Position = UDim2.new(0.5, -110, 0.5, -65) 
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35) 
MainFrame.BorderSizePixel = 2 
MainFrame.Active = true 
MainFrame.Draggable = true 
MainFrame.Parent = ScreenGui 
 
local Title = Instance.new("TextLabel") 
Title.Size = UDim2.new(1, 0, 0, 35) 
Title.BackgroundColor3 = Color3.fromRGB(20, 20, 20) 
Title.Text = "小傑腳本 V2" 
Title.TextColor3 = Color3.new(1, 1, 1) 
Title.Font = Enum.Font.GothamBold 
Title.Parent = MainFrame 
 
local Toggle = Instance.new("TextButton") 
Toggle.Size = UDim2.new(0.8, 0, 0, 40) 
Toggle.Position = UDim2.new(0.1, 0, 0.45, 0) 
Toggle.BackgroundColor3 = Color3.fromRGB(60, 60, 60) 
Toggle.Text = "自瞄狀態: OFF" 
Toggle.TextColor3 = Color3.new(1, 1, 1) 
Toggle.Parent = MainFrame 
 
Toggle.MouseButton1Click:Connect(function() 
    SilentAimPlayersEnabled = not SilentAimPlayersEnabled 
    Toggle.Text = SilentAimPlayersEnabled and "自瞄狀態: ON" or "自瞄狀態: OFF" 
    Toggle.BackgroundColor3 = SilentAimPlayersEnabled and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(60, 60, 60) 
end) 
 
UserInputService.InputBegan:Connect(function(input, gpe) 
    if not gpe and input.KeyCode == Enum.KeyCode.Insert then 
        ScreenGui.Enabled = not ScreenGui.Enabled 
    end 
end) 
 
print("小傑腳本 修正完成！按 Insert 鍵隱藏/顯示選單。")
