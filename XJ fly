local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer
local flying = false
local speed = 50
local minSpeed = 1
local maxSpeed = 10000
local attachment, lv, ao, character, humanoid, root
local lockedTracks = {}
local animateScript = nil
local noclipEnabled = false

local existing = player:WaitForChild("PlayerGui"):FindFirstChild("XJ Fly")
if existing then existing:Destroy() end

local m = Instance.new("ScreenGui")
local f = Instance.new("Frame")
local up = Instance.new("TextButton")
local down = Instance.new("TextButton")
local o = Instance.new("TextButton")
local t = Instance.new("TextLabel")
local p = Instance.new("TextButton")
local s = Instance.new("TextBox")
local n = Instance.new("TextButton")
local c = Instance.new("TextButton")
local tpBtn = Instance.new("TextButton")
local h = Instance.new("TextButton")
local nc = Instance.new("TextButton")
local h2 = Instance.new("TextButton")

m.Name = "RJRFly"
m.Parent = player:WaitForChild("PlayerGui")
m.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
m.ResetOnSpawn = false

f.Parent = m
f.BackgroundColor3 = Color3.fromRGB(163, 255, 137)
f.BorderColor3 = Color3.fromRGB(103, 221, 213)
f.Position = UDim2.new(0.100320168, 0, 0.379746825, 0)
f.Size = UDim2.new(0, 190, 0, 57)
f.Active = true
f.Draggable = true

local function b(x, y, z, q)
	x.Name = y
	x.Parent = f
	x.Position = z
	x.Size = q
	x.Font = Enum.Font.SourceSans
	x.Text = y
	x.TextColor3 = Color3.fromRGB(0, 0, 0)
	x.TextSize = 14
	x.TextScaled = true
	x.TextWrapped = true
end

local function requestEntrance(pos)
    local args = {
        [1] = "requestEntrance",
        [2] = pos
    }
    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
end

local World2 = game.PlaceId == 4442272183 or game.PlaceId == 79091703265657
local World3 = game.PlaceId == 7449423635 or game.PlaceId == 100117331123089

local function GetUpTarget()
    if World2 then return "鬼船", Vector3.new(923, 126, 32852)
    elseif World3 then return "海龜", Vector3.new(-5060.41, 316.43, -3192.30) end
    return "鬼船", Vector3.new(923, 126, 32852)
end

local function GetDownTarget()
    if World2 then return "豪宅", Vector3.new(-390, 332, 673)
    elseif World3 then return "九頭蛇", Vector3.new(-5027.03, 316.43, -3206.07) end
    return "豪宅", Vector3.new(-390, 332, 673)
end

local function GetTPTarget()
    if World2 then return "墓地", Vector3.new(-6509, 83, -133)
    elseif World3 then return "司法", Vector3.new(-5096.48, 316.43, -3177.91) end
    return "墓地", Vector3.new(-6509, 83, -133)
end

local upName, upPos = GetUpTarget()
local downName, downPos = GetDownTarget()
local tpName = select(1, GetTPTarget())

-- ========== 第一排 ==========
b(up, "↑", UDim2.new(0, 0, 0, 0), UDim2.new(0, 44, 0, 28))
up.BackgroundColor3 = Color3.fromRGB(79, 255, 152)
up.Text = upName

p.Name = "plus"
p.Parent = f
p.BackgroundColor3 = Color3.fromRGB(133, 145, 255)
p.Position = UDim2.new(0, 44, 0, 0)
p.Size = UDim2.new(0, 45, 0, 28)
p.Font = Enum.Font.SourceSans
p.Text = "+5"
p.TextColor3 = Color3.fromRGB(0, 0, 0)
p.TextScaled = true
p.TextSize = 14
p.TextWrapped = true

t.Parent = f
t.BackgroundColor3 = Color3.fromRGB(242, 60, 255)
t.Position = UDim2.new(0, 89, 0, 0)
t.Size = UDim2.new(0, 101, 0, 28)
t.Font = Enum.Font.SourceSans
t.Text = "XJ FLY"
t.TextColor3 = Color3.fromRGB(0, 0, 0)
t.TextScaled = true
t.TextSize = 14
t.TextWrapped = true

-- ========== 第二排 ==========
b(down, "↓", UDim2.new(0, 0, 0, 28), UDim2.new(0, 44, 0, 28))
down.BackgroundColor3 = Color3.fromRGB(215, 255, 121)
down.Text = downName

n.Name = "mine"
n.Parent = f
n.BackgroundColor3 = Color3.fromRGB(123, 255, 247)
n.Position = UDim2.new(0, 44, 0, 28)
n.Size = UDim2.new(0, 45, 0, 28)
n.Font = Enum.Font.SourceSans
n.Text = "-5"
n.TextColor3 = Color3.fromRGB(0, 0, 0)
n.TextScaled = true
n.TextSize = 14
n.TextWrapped = true

s.Parent = f
s.Name = "speed"
s.BackgroundColor3 = Color3.fromRGB(255, 85, 0)
s.Position = UDim2.new(0, 89, 0, 28)
s.Size = UDim2.new(0, 45, 0, 28)
s.Font = Enum.Font.SourceSans
s.Text = tostring(speed)
s.TextColor3 = Color3.fromRGB(0, 0, 0)
s.TextScaled = true
s.TextSize = 14
s.TextWrapped = true
s.ClearTextOnFocus = false
s.TextEditable = true
s.PlaceholderText = "1-150"

o.Name = "onoff"
o.Parent = f
o.BackgroundColor3 = Color3.fromRGB(255, 249, 74)
o.Position = UDim2.new(0, 134, 0, 28)
o.Size = UDim2.new(0, 56, 0, 28)
o.Font = Enum.Font.SourceSans
o.Text = "開始"
o.TextColor3 = Color3.fromRGB(0, 0, 0)
o.TextScaled = true
o.TextSize = 14
o.TextWrapped = true

-- ========== 底部按鈕 ==========
tpBtn.Name = "tp"
tpBtn.Parent = f
tpBtn.BackgroundColor3 = Color3.fromRGB(150, 200, 255)
tpBtn.Font = Enum.Font.SourceSans
tpBtn.Size = UDim2.new(0, 44, 0, 28)
tpBtn.Text = tpName
tpBtn.TextSize = 30
tpBtn.Position = UDim2.new(0, 0, -1, 28)

c.Name = "Close"
c.Parent = f
c.BackgroundColor3 = Color3.fromRGB(225, 25, 0)
c.Font = Enum.Font.SourceSans
c.Size = UDim2.new(0, 45.5, 0, 28)
c.Text = "關閉"
c.TextSize = 30
c.Position = UDim2.new(0, 44, -1, 28)

h.Name = "hide"
h.Parent = f
h.BackgroundColor3 = Color3.fromRGB(192, 150, 230)
h.Font = Enum.Font.SourceSans
h.Size = UDim2.new(0, 45, 0, 28)
h.Text = "隱藏"
h.TextSize = 30
h.Position = UDim2.new(0, 89.5, -1, 28)

nc.Name = "noclip"
nc.Parent = f
nc.BackgroundColor3 = Color3.fromRGB(255, 100, 100)
nc.Font = Enum.Font.SourceSans
nc.Size = UDim2.new(0, 44, 0, 28)
nc.Text = "穿牆"
nc.TextSize = 30
nc.Position = UDim2.new(0, 134.5, -1, 28)

h2.Name = "show"
h2.Parent = f
h2.BackgroundColor3 = Color3.fromRGB(192, 150, 230)
h2.Font = Enum.Font.SourceSans
h2.Size = UDim2.new(0, 45, 0, 28)
h2.Text = "顯示"
h2.TextSize = 30
h2.Position = UDim2.new(0, 89.5, -1, 28)
h2.Visible = false

local function clampSpeed(v)
	v = math.floor(tonumber(v) or speed)
	if v < minSpeed then v = minSpeed end
	if v > maxSpeed then v = maxSpeed end
	return v
end

local function setSpeed(v)
	speed = clampSpeed(v)
	s.Text = tostring(speed)
end

local function changeSpeed(delta)
	setSpeed(speed + delta)
end

local noclipConn = nil

local function ToggleNoclip()
    noclipEnabled = not noclipEnabled
    if noclipEnabled then
        nc.Text = "穿牆"
        nc.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
        noclipConn = RunService.Stepped:Connect(function()
            local char = player.Character
            if char then
                for _, part in pairs(char:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end)
    else
        nc.Text = "穿牆"
        nc.BackgroundColor3 = Color3.fromRGB(255, 100, 100)
        if noclipConn then noclipConn:Disconnect(); noclipConn = nil end
        local char = player.Character
        if char then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = true end
            end
        end
    end
end

local function clearPhysics()
	if attachment then attachment:Destroy(); attachment = nil end
	lv = nil
	ao = nil
end

local function lockPose()
	if not humanoid or not character then return end
	animateScript = character:FindFirstChild("Animate")
	if animateScript then animateScript.Disabled = true end
	lockedTracks = {}
	for _, track in ipairs(humanoid:GetPlayingAnimationTracks()) do
		track:AdjustSpeed(0)
		table.insert(lockedTracks, track)
	end
end

local function unlockPose()
	for _, track in ipairs(lockedTracks) do
		if track then pcall(function() track:AdjustSpeed(1) end) end
	end
	lockedTracks = {}
	if animateScript then
		animateScript.Disabled = false
		animateScript = nil
	elseif character then
		local a = character:FindFirstChild("Animate")
		if a then a.Disabled = false end
	end
end

local function keepPoseLocked()
	if not flying or not humanoid then return end
	for _, track in ipairs(humanoid:GetPlayingAnimationTracks()) do
		if track.Speed ~= 0 then track:AdjustSpeed(0) end
	end
end

local function setupPhysics()
	character = player.Character or player.CharacterAdded:Wait()
	humanoid = character:WaitForChild("Humanoid")
	root = character:WaitForChild("HumanoidRootPart")
	clearPhysics()
	if not flying then return end
	lockPose()
	humanoid.PlatformStand = true
	attachment = Instance.new("Attachment")
	attachment.Parent = root
	lv = Instance.new("LinearVelocity")
	lv.MaxForce = 9e9
	lv.VectorVelocity = Vector3.zero
	lv.Attachment0 = attachment
	lv.RelativeTo = Enum.ActuatorRelativeTo.World
	lv.Parent = attachment
	ao = Instance.new("AlignOrientation")
	ao.MaxTorque = 9e9
	ao.Responsiveness = 200
	ao.Mode = Enum.OrientationAlignmentMode.OneAttachment
	ao.Attachment0 = attachment
	ao.Parent = attachment
end

local function stopFly()
	flying = false
	o.Text = "開始"
	if humanoid then humanoid.PlatformStand = false end
	clearPhysics()
	unlockPose()
end

local function startFly()
	flying = true
	o.Text = "停止"
	setupPhysics()
end

local function toggleFly()
	if flying then stopFly() else startFly() end
end

player.CharacterAdded:Connect(function()
	task.wait(0.15)
	character = player.Character
	humanoid = character and character:FindFirstChildOfClass("Humanoid")
	root = character and character:FindFirstChild("HumanoidRootPart")
	if flying then setupPhysics() end
end)

local PlayerModule = require(player.PlayerScripts:WaitForChild("PlayerModule"))
local Controls = PlayerModule:GetControls()

local conn = RunService.RenderStepped:Connect(function()
	if not flying or not root or not lv or not ao or not workspace.CurrentCamera then return end
	keepPoseLocked()
	local cam = workspace.CurrentCamera
	local moveVector = Controls:GetMoveVector()
	local direction = Vector3.zero
	if moveVector.Magnitude > 0 then
		direction = (cam.CFrame.LookVector * -moveVector.Z) + (cam.CFrame.RightVector * moveVector.X)
	end
	if direction.Magnitude > 0 then
		lv.VectorVelocity = direction.Unit * speed
	else
		lv.VectorVelocity = Vector3.zero
	end
	ao.CFrame = cam.CFrame
end)

o.MouseButton1Click:Connect(toggleFly)

up.MouseButton1Click:Connect(function()
    local _, pos = GetUpTarget()
    requestEntrance(pos)
end)

down.MouseButton1Click:Connect(function()
    local _, pos = GetDownTarget()
    requestEntrance(pos)
end)

tpBtn.MouseButton1Click:Connect(function()
    local _, pos = GetTPTarget()
    requestEntrance(pos)
end)

nc.MouseButton1Click:Connect(ToggleNoclip)

local plusHoldConn = nil
local plusHolding = false

local function stopPlusHold()
    plusHolding = false
    if plusHoldConn then plusHoldConn:Disconnect(); plusHoldConn = nil end
end

p.MouseButton1Down:Connect(function()
    if plusHolding then return end
    plusHolding = true
    changeSpeed(5)
    task.delay(0.9, function()
        if plusHolding and not plusHoldConn then
            plusHoldConn = RunService.Heartbeat:Connect(function()
                changeSpeed(5)
            end)
        end
    end)
end)

local minusHoldConn = nil
local minusHolding = false

local function stopMinusHold()
    minusHolding = false
    if minusHoldConn then minusHoldConn:Disconnect(); minusHoldConn = nil end
end

n.MouseButton1Down:Connect(function()
    if minusHolding then return end
    minusHolding = true
    changeSpeed(-5)
    task.delay(0.9, function()
        if minusHolding and not minusHoldConn then
            minusHoldConn = RunService.Heartbeat:Connect(function()
                changeSpeed(-5)
            end)
        end
    end)
end)

UIS.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        stopPlusHold()
        stopMinusHold()
    end
end)

s.FocusLost:Connect(function()
	if s.Text == "" then s.Text = tostring(speed); return end
	local ns = tonumber(s.Text)
	if ns then setSpeed(ns) else s.Text = tostring(speed) end
end)

c.MouseButton1Click:Connect(function()
	stopFly()
	if conn then conn:Disconnect() end
	if noclipConn then noclipConn:Disconnect() end
	stopPlusHold()
	stopMinusHold()
	m:Destroy()
end)

h.MouseButton1Click:Connect(function()
	up.Visible = false
	down.Visible = false
	o.Visible = false
	p.Visible = false
	s.Visible = false
	n.Visible = false
	tpBtn.Visible = false
	h.Visible = false
	nc.Visible = false
	c.Visible = false
	h2.Visible = true
	f.BackgroundTransparency = 1
end)

h2.MouseButton1Click:Connect(function()
	up.Visible = true
	down.Visible = true
	o.Visible = true
	p.Visible = true
	s.Visible = true
	n.Visible = true
	tpBtn.Visible = true
	h.Visible = true
	nc.Visible = true
	c.Visible = true
	h2.Visible = false
	f.BackgroundTransparency = 0
end)
